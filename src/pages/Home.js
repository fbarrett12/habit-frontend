import React from 'react'
import { useSelector } from 'react-redux'
import Orb from '../components/Orb'

const Home = () => {
    return(
        <div>
            <Orb />
        </div>
    )
}

export default Home